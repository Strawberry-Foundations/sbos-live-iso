if loadfont /boot/grub/font.pf2 ; then
 set gfxmode=auto
 insmod efi_gop
 insmod efi_uga
 insmod gfxterm
 terminal_output gfxterm
fi

set menu_color_normal=white/black
set menu_color_highlight=black/light-gray

set timeout=5

menuentry "Install StrawberryOS" {
 echo    'Starting StrawberryOS'
 set gfxpayload=keep
 echo    'Loading Linux kernel ...'
 linux	KERNEL_LIVE APPEND_LIVE quiet ---
 echo    'Loading initramfs ...'
 initrd INITRD_LIVE
}

menuentry "Install StrawberryOS (Safe Graphics)" {
 echo    'Starting StrawberryOS with safe graphics'
 set gfxpayload=keep
 echo    'Loading Linux kernel ...'
 linux	KERNEL_LIVE APPEND_LIVE nomodeset quiet ---
 echo    'Loading initramfs ...'
 initrd INITRD_LIVE
}

grub_platform
if [ "$grub_platform" = "efi" ]; then
menuentry 'Boot from next volume' {
 exit
}
menuentry 'UEFI Firmware Settings' {
 fwsetup
}
fi
